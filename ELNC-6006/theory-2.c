#include<stdio.h>

int main(void)
{
    int temperature;
    int limit;

    printf("Enter the temperature in Celsius: ");
    scanf("%d,%d", &temperature, &limit);

    printf("the temperature is ");
}